Orchestrator(
		input String channel rotaryTableStatus, capperStatus;
		output Boolean channel rotaryTableRequest, capperRequest;
		)
->{
	System.out.println("Hello Machine");
	{
		while(true) {
			send rotaryTableRequest(true);
			pause;
		}
	}
}